Linux-Kernel-Mem-Analysis
=========================

In order to analyze page fault data in the kernel we needed to modify the kernel's code, create a kernel module to work with the data, and finally post process the data.